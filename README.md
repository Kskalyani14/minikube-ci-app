# minikube-ci-app
Minikube CI with GitHub Actions
Automates app testing using Minikube in GitHub Actions. On every PR, the workflow builds the Docker image, deploys it to Minikube, and runs tests to verify the deployment.

